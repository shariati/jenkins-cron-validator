{
  "name": "jenkins-cron-validator",
  "version": "0.0.1",
  "description": "Parser/builder for Jenkins Cron format",
  "main": "dist/index.js",
  "scripts": {
    "lint": "tslint --fix --project tsconfig.json lib/**/*.ts",
    "format": "prettier --write lib/**/*.ts",
    "build": "rimraf dist & webpack --mode production",
    "test": "mocha --reporter spec"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shariati/jenkins-cron-validator.git"
  },
  "keywords": ["jenkins", "cron", "validate", "validator", "syntax", "format", "parser", "builder"],
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/shariati/jenkins-cron-validator/issues"
  },
  "homepage": "https://github.com/shariati/jenkins-cron-validator#readme",
  "devDependencies": {
    "awesome-typescript-loader": "^3.5.0",
    "chai": "^4.1.2",
    "mocha": "^5.0.1",
    "prettier": "^1.10.2",
    "rimraf": "^2.6.2",
    "tslint": "^5.9.1",
    "tslint-config-airbnb": "^5.7.0",
    "tslint-config-prettier": "^1.9.0",
    "typescript": "^2.7.2",
    "webpack": "^4.0.0",
    "webpack-cli": "^2.0.8"
  }
}
